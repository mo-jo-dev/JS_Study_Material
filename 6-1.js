// CRUD - Create Read Update Delete



// npx - NPX stands for Node Package eXecute. It is simply an NPM package runner. It allows developers to execute any Javascript Package available on the NPM registry without even installing it.

// nodemon - used to automatically update the server information, if there is a change in the code.


// In the script, we can write the command that we wish to directly execute,, this helps us to prevent the writing of long commands again and again
// Just type npm __name of your command in scripts in package.json__


// body-parser : a package by which we could access the request body...

// middlewares - We can put some logic before the API gets hit. This could check/do a lot of things
// we can setup a layer/ middle layer by using ____app.use()____